# 核心依赖 - 所有脚本都需要
requests>=2.31.0
urllib3>=2.2.0

# 雨晨iOS专用 - HTML解析
beautifulsoup4>=4.12.0
lxml>=4.9.0
pillow>=10.0.0
pytesseract>=0.3.10

# 尚香书苑曾试用 curl_cffi 绕过 Cloudflare 指纹质询，但实测站点按机房 IP 信誉弹 JS 质询、
# 换指纹无效（详见 README），故停用。
# curl_cffi>=0.7.0
